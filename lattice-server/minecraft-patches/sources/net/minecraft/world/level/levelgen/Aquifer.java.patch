--- a/net/minecraft/world/level/levelgen/Aquifer.java
+++ b/net/minecraft/world/level/levelgen/Aquifer.java
@@ -11,7 +_,6 @@
 import net.minecraft.world.level.block.Blocks;
 import net.minecraft.world.level.block.state.BlockState;
 import net.minecraft.world.level.dimension.DimensionType;
-import org.apache.commons.lang3.mutable.MutableDouble;
 import org.jspecify.annotations.Nullable;
 
 public interface Aquifer {
@@ -78,6 +_,9 @@
         private static final int MAX_CELL_SAMPLE_X = 1;
         private static final int MAX_CELL_SAMPLE_Y = 1;
         private static final int MAX_CELL_SAMPLE_Z = 1;
+        private static final int CENTER_COUNT = 12;
+        private static final int CENTER_CACHE_SLOTS = 16;
+        private static final int CENTER_CACHE_STRIDE = CENTER_COUNT * 4;
         private final NoiseChunk noiseChunk;
         private final DensityFunction barrierNoise;
         private final DensityFunction fluidLevelFloodednessNoise;
@@ -90,14 +_,17 @@
         private final DensityFunction erosion;
         private final DensityFunction depth;
         private boolean shouldScheduleFluidUpdate;
+        private double barrierNoiseValue;
         private final int skipSamplingAboveY;
         private final int minGridX;
         private final int minGridY;
         private final int minGridZ;
         private final int gridSizeX;
         private final int gridSizeZ;
-        private static final int[][] SURFACE_SAMPLING_OFFSETS_IN_CHUNKS = new int[][]{
-            {0, 0}, {-2, -1}, {-1, -1}, {0, -1}, {1, -1}, {-3, 0}, {-2, 0}, {-1, 0}, {1, 0}, {-2, 1}, {-1, 1}, {0, 1}, {1, 1}
+        private final int[] centerCacheKeys = new int[CENTER_CACHE_SLOTS];
+        private final int[] centerCache = new int[CENTER_CACHE_SLOTS * CENTER_CACHE_STRIDE];
+        private static final int[] SURFACE_SAMPLING_OFFSETS_IN_BLOCKS = new int[]{
+            0, 0, -32, -16, -16, -16, 0, -16, 16, -16, -48, 0, -32, 0, -16, 0, 16, 0, -32, 16, -16, 16, 0, 16, 16, 16
         };
 
         NoiseBasedAquifer(
@@ -131,6 +_,7 @@
             this.aquiferCache = new Aquifer.FluidStatus[i4];
             this.aquiferLocationCache = new long[i4];
             Arrays.fill(this.aquiferLocationCache, Long.MAX_VALUE);
+            Arrays.fill(this.centerCacheKeys, -1);
             int i5 = this.adjustSurfaceLevel(
                 noiseChunk.maxPreliminarySurfaceLevel(fromGridX(this.minGridX, 0), fromGridZ(this.minGridZ, 0), fromGridX(i, 9), fromGridZ(i3, 9))
             );
@@ -155,10 +_,11 @@
                 int i1 = context.blockY();
                 int i2 = context.blockZ();
                 Aquifer.FluidStatus fluidStatus = this.globalFluidPicker.computeFluid(i, i1, i2);
+                BlockState fluidBlockState = fluidStatus.at(i1);
                 if (i1 > this.skipSamplingAboveY) {
                     this.shouldScheduleFluidUpdate = false;
-                    return fluidStatus.at(i1);
-                } else if (fluidStatus.at(i1).is(Blocks.LAVA)) {
+                    return fluidBlockState;
+                } else if (fluidBlockState.is(Blocks.LAVA)) {
                     this.shouldScheduleFluidUpdate = false;
                     return SharedConstants.DEBUG_DISABLE_FLUID_GENERATION ? Blocks.AIR.defaultBlockState() : Blocks.LAVA.defaultBlockState();
                 } else {
@@ -174,57 +_,37 @@
                     int i12 = 0;
                     int i13 = 0;
 
-                    for (int i14 = 0; i14 <= 1; i14++) {
-                        for (int i15 = -1; i15 <= 1; i15++) {
-                            for (int i16 = 0; i16 <= 1; i16++) {
-                                int i17 = i3 + i14;
-                                int i18 = i4 + i15;
-                                int i19 = i5 + i16;
-                                int index = this.getIndex(i17, i18, i19);
-                                long l = this.aquiferLocationCache[index];
-                                long l1;
-                                if (l != Long.MAX_VALUE) {
-                                    l1 = l;
-                                } else {
-                                    RandomSource randomSource = this.positionalRandomFactory.at(i17, i18, i19);
-                                    l1 = BlockPos.asLong(
-                                        fromGridX(i17, randomSource.nextInt(10)),
-                                        fromGridY(i18, randomSource.nextInt(9)),
-                                        fromGridZ(i19, randomSource.nextInt(10))
-                                    );
-                                    this.aquiferLocationCache[index] = l1;
-                                }
-
-                                int i20 = BlockPos.getX(l1) - i;
-                                int i21 = BlockPos.getY(l1) - i1;
-                                int i22 = BlockPos.getZ(l1) - i2;
-                                int i23 = i20 * i20 + i21 * i21 + i22 * i22;
-                                if (i6 >= i23) {
-                                    i13 = i12;
-                                    i12 = i11;
-                                    i11 = i10;
-                                    i10 = index;
-                                    i9 = i8;
-                                    i8 = i7;
-                                    i7 = i6;
-                                    i6 = i23;
-                                } else if (i7 >= i23) {
-                                    i13 = i12;
-                                    i12 = i11;
-                                    i11 = index;
-                                    i9 = i8;
-                                    i8 = i7;
-                                    i7 = i23;
-                                } else if (i8 >= i23) {
-                                    i13 = i12;
-                                    i12 = index;
-                                    i9 = i8;
-                                    i8 = i23;
-                                } else if (i9 >= i23) {
-                                    i13 = index;
-                                    i9 = i23;
-                                }
-                            }
+                    int centerOffset = this.getAquiferCenters(i3, i4, i5);
+                    for (int i14 = 0; i14 < CENTER_COUNT; i14++) {
+                        int i20 = this.centerCache[centerOffset++] - i;
+                        int i21 = this.centerCache[centerOffset++] - i1;
+                        int i22 = this.centerCache[centerOffset++] - i2;
+                        int index = this.centerCache[centerOffset++];
+                        int i23 = i20 * i20 + i21 * i21 + i22 * i22;
+                        if (i6 >= i23) {
+                            i13 = i12;
+                            i12 = i11;
+                            i11 = i10;
+                            i10 = index;
+                            i9 = i8;
+                            i8 = i7;
+                            i7 = i6;
+                            i6 = i23;
+                        } else if (i7 >= i23) {
+                            i13 = i12;
+                            i12 = i11;
+                            i11 = index;
+                            i9 = i8;
+                            i8 = i7;
+                            i7 = i23;
+                        } else if (i8 >= i23) {
+                            i13 = i12;
+                            i12 = index;
+                            i9 = i8;
+                            i8 = i23;
+                        } else if (i9 >= i23) {
+                            i13 = index;
+                            i9 = i23;
                         }
                     }
 
@@ -245,9 +_,9 @@
                         this.shouldScheduleFluidUpdate = true;
                         return blockState1;
                     } else {
-                        MutableDouble mutableDouble = new MutableDouble(Double.NaN);
+                        this.barrierNoiseValue = Double.NaN;
                         Aquifer.FluidStatus aquiferStatus2 = this.getAquiferStatus(i11);
-                        double d1 = d * this.calculatePressure(context, mutableDouble, aquiferStatus, aquiferStatus2);
+                        double d1 = d * this.calculatePressure(context, aquiferStatus, aquiferStatus2);
                         if (substance + d1 > 0.0) {
                             this.shouldScheduleFluidUpdate = false;
                             return null;
@@ -255,7 +_,7 @@
                             Aquifer.FluidStatus aquiferStatus3 = this.getAquiferStatus(i12);
                             double d2 = similarity(i6, i8);
                             if (d2 > 0.0) {
-                                double d3 = d * d2 * this.calculatePressure(context, mutableDouble, aquiferStatus, aquiferStatus3);
+                                double d3 = d * d2 * this.calculatePressure(context, aquiferStatus, aquiferStatus3);
                                 if (substance + d3 > 0.0) {
                                     this.shouldScheduleFluidUpdate = false;
                                     return null;
@@ -264,7 +_,7 @@
 
                             double d3 = similarity(i7, i8);
                             if (d3 > 0.0) {
-                                double d4 = d * d3 * this.calculatePressure(context, mutableDouble, aquiferStatus2, aquiferStatus3);
+                                double d4 = d * d3 * this.calculatePressure(context, aquiferStatus2, aquiferStatus3);
                                 if (substance + d4 > 0.0) {
                                     this.shouldScheduleFluidUpdate = false;
                                     return null;
@@ -294,14 +_,59 @@
             return this.shouldScheduleFluidUpdate;
         }
 
+        private int getAquiferCenters(int baseGridX, int baseGridY, int baseGridZ) {
+            int key = this.getIndex(baseGridX, baseGridY, baseGridZ);
+            int slot = key & (CENTER_CACHE_SLOTS - 1);
+            int offset = slot * CENTER_CACHE_STRIDE;
+            if (this.centerCacheKeys[slot] == key) {
+                return offset;
+            }
+
+            int writeOffset = offset;
+            int gridStrideY = this.gridSizeZ * this.gridSizeX;
+            int gridStrideZ = this.gridSizeX;
+            for (int gridOffsetX = 0; gridOffsetX <= 1; gridOffsetX++) {
+                for (int gridOffsetY = -1; gridOffsetY <= 1; gridOffsetY++) {
+                    for (int gridOffsetZ = 0; gridOffsetZ <= 1; gridOffsetZ++) {
+                        int gridX = baseGridX + gridOffsetX;
+                        int gridY = baseGridY + gridOffsetY;
+                        int gridZ = baseGridZ + gridOffsetZ;
+                        int index = key + gridOffsetX + gridOffsetY * gridStrideY + gridOffsetZ * gridStrideZ;
+                        long location = this.aquiferLocationCache[index];
+                        if (location == Long.MAX_VALUE) {
+                            int offsets;
+                            if (this.positionalRandomFactory instanceof XoroshiroRandomSource.XoroshiroPositionalRandomFactory xoroshiro) {
+                                offsets = xoroshiro.lattice$sampleAquiferOffsets(gridX, gridY, gridZ);
+                            } else {
+                                RandomSource randomSource = this.positionalRandomFactory.at(gridX, gridY, gridZ);
+                                offsets = randomSource.nextInt(10) | randomSource.nextInt(9) << 4 | randomSource.nextInt(10) << 8;
+                            }
+
+                            location = BlockPos.asLong(
+                                fromGridX(gridX, offsets & 15),
+                                fromGridY(gridY, offsets >> 4 & 15),
+                                fromGridZ(gridZ, offsets >> 8 & 15)
+                            );
+                            this.aquiferLocationCache[index] = location;
+                        }
+
+                        this.centerCache[writeOffset++] = BlockPos.getX(location);
+                        this.centerCache[writeOffset++] = BlockPos.getY(location);
+                        this.centerCache[writeOffset++] = BlockPos.getZ(location);
+                        this.centerCache[writeOffset++] = index;
+                    }
+                }
+            }
+            this.centerCacheKeys[slot] = key;
+            return offset;
+        }
+
         private static double similarity(int firstDistance, int secondDistance) {
             double d = 25.0;
             return 1.0 - (secondDistance - firstDistance) / 25.0;
         }
 
-        private double calculatePressure(
-            DensityFunction.FunctionContext context, MutableDouble substance, Aquifer.FluidStatus firstFluid, Aquifer.FluidStatus secondFluid
-        ) {
+        private double calculatePressure(DensityFunction.FunctionContext context, Aquifer.FluidStatus firstFluid, Aquifer.FluidStatus secondFluid) {
             int i = context.blockY();
             BlockState blockState = firstFluid.at(i);
             BlockState blockState1 = secondFluid.at(i);
@@ -340,10 +_,10 @@
                     double d10x = 2.0;
                     double d12;
                     if (!(d11 < -2.0) && !(d11 > 2.0)) {
-                        double d13 = substance.doubleValue();
+                        double d13 = this.barrierNoiseValue;
                         if (Double.isNaN(d13)) {
                             double d14 = this.barrierNoise.compute(context);
-                            substance.setValue(d14);
+                            this.barrierNoiseValue = d14;
                             d12 = d14;
                         } else {
                             d12 = d13;
@@ -402,12 +_,14 @@
             int i2 = y - 12;
             boolean flag = false;
 
-            for (int[] ints : SURFACE_SAMPLING_OFFSETS_IN_CHUNKS) {
-                int i3 = x + SectionPos.sectionToBlockCoord(ints[0]);
-                int i4 = z + SectionPos.sectionToBlockCoord(ints[1]);
+            for (int offset = 0; offset < SURFACE_SAMPLING_OFFSETS_IN_BLOCKS.length; offset += 2) {
+                int offsetX = SURFACE_SAMPLING_OFFSETS_IN_BLOCKS[offset];
+                int offsetZ = SURFACE_SAMPLING_OFFSETS_IN_BLOCKS[offset + 1];
+                int i3 = x + offsetX;
+                int i4 = z + offsetZ;
                 int i5 = this.noiseChunk.preliminarySurfaceLevel(i3, i4);
                 int i6 = this.adjustSurfaceLevel(i5);
-                boolean flag1 = ints[0] == 0 && ints[1] == 0;
+                boolean flag1 = offsetX == 0 && offsetZ == 0;
                 if (flag1 && i2 > i6) {
                     return fluidStatus;
                 }
