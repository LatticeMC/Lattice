--- a/net/minecraft/world/level/levelgen/NoiseChunk.java
+++ b/net/minecraft/world/level/levelgen/NoiseChunk.java
@@ -1,9 +_,9 @@
 package net.minecraft.world.level.levelgen;
 
 import com.google.common.collect.Lists;
-import it.unimi.dsi.fastutil.longs.Long2IntMap;
+import com.latticemc.lattice.nativelib.NativeClimateGrid;
+import com.latticemc.lattice.nativelib.NativeDensityFunction;
 import it.unimi.dsi.fastutil.longs.Long2IntOpenHashMap;
-import java.util.ArrayList;
 import java.util.Arrays;
 import java.util.HashMap;
 import java.util.List;
@@ -18,7 +_,6 @@
 import net.minecraft.world.level.block.state.BlockState;
 import net.minecraft.world.level.chunk.ChunkAccess;
 import net.minecraft.world.level.levelgen.blending.Blender;
-import net.minecraft.world.level.levelgen.material.MaterialRuleList;
 import org.jspecify.annotations.Nullable;
 
 public class NoiseChunk implements DensityFunction.ContextProvider, DensityFunction.FunctionContext {
@@ -30,12 +_,15 @@
     final int firstNoiseX;
     final int firstNoiseZ;
     final List<NoiseChunk.NoiseInterpolator> interpolators;
+    final NoiseChunk.NoiseInterpolator[] interpolatorArray;
     final List<NoiseChunk.CacheAllInCell> cellCaches;
     private final Map<DensityFunction, DensityFunction> wrapped = new HashMap<>();
-    private final Long2IntMap preliminarySurfaceLevelCache = new Long2IntOpenHashMap();
+    private final Long2IntOpenHashMap preliminarySurfaceLevelCache = new Long2IntOpenHashMap();
     private final Aquifer aquifer;
     private final DensityFunction preliminarySurfaceLevel;
-    private final NoiseChunk.BlockStateFiller blockStateRule;
+    private final DensityFunction finalDensity;
+    private final DensityFunctions.@Nullable BeardifierOrMarker activeBeardifier;
+    private final NoiseChunk.@Nullable BlockStateFiller oreVeinifier;
     private final Blender blender;
     private final NoiseChunk.FlatCache blendAlpha;
     private final NoiseChunk.FlatCache blendOffset;
@@ -104,6 +_,7 @@
         Blender blendifier
     ) {
         this.cellWidth = noiseSettings.getCellWidth();
+        this.preliminarySurfaceLevelCache.defaultReturnValue(Integer.MIN_VALUE);
         this.cellHeight = noiseSettings.getCellHeight();
         this.cellCountXZ = cellCountXZ;
         this.cellCountY = Mth.floorDiv(noiseSettings.height(), this.cellHeight);
@@ -117,6 +_,7 @@
         this.noiseSizeXZ = QuartPos.fromBlock(cellCountXZ * this.cellWidth);
         this.blender = blendifier;
         this.beardifier = beardifier;
+        this.activeBeardifier = beardifier == Beardifier.EMPTY || beardifier == DensityFunctions.BeardifierMarker.INSTANCE ? null : beardifier;
         this.blendAlpha = new NoiseChunk.FlatCache(new NoiseChunk.BlendAlpha(), false);
         this.blendOffset = new NoiseChunk.FlatCache(new NoiseChunk.BlendOffset(), false);
         if (!blendifier.isEmpty()) {
@@ -150,33 +_,64 @@
             );
         }
 
-        List<NoiseChunk.BlockStateFiller> list = new ArrayList<>();
-        DensityFunction densityFunction = DensityFunctions.cacheAllInCell(
-                DensityFunctions.add(noiseRouter1.finalDensity(), DensityFunctions.BeardifierMarker.INSTANCE)
-            )
+        // Keep the structure blend in the pointwise path while allowing the
+        // expensive terrain density subtree to use the native cell-column path.
+        this.finalDensity = DensityFunctions.cacheAllInCell(noiseRouter1.finalDensity())
             .mapAll(this::wrap);
-        list.add(context -> this.aquifer.computeSubstance(context, densityFunction.compute(context)));
         if (noiseGeneratorSettings.oreVeinsEnabled()) {
-            list.add(OreVeinifier.create(noiseRouter1.veinToggle(), noiseRouter1.veinRidged(), noiseRouter1.veinGap(), random.oreRandom()));
+            this.oreVeinifier = OreVeinifier.create(
+                noiseRouter1.veinToggle(), noiseRouter1.veinRidged(), noiseRouter1.veinGap(), random.oreRandom()
+            );
+        } else {
+            this.oreVeinifier = null;
         }
 
-        this.blockStateRule = new MaterialRuleList(list.toArray(new NoiseChunk.BlockStateFiller[0]));
+        this.interpolatorArray = this.interpolators.toArray(new NoiseChunk.NoiseInterpolator[0]);
     }
 
     protected Climate.Sampler cachedClimateSampler(NoiseRouter noiseRouter, List<Climate.ParameterPoint> points) {
-        return new Climate.Sampler(
+        DensityFunction[] functions = new DensityFunction[]{
             noiseRouter.temperature().mapAll(this::wrap),
             noiseRouter.vegetation().mapAll(this::wrap),
             noiseRouter.continents().mapAll(this::wrap),
             noiseRouter.erosion().mapAll(this::wrap),
             noiseRouter.depth().mapAll(this::wrap),
-            noiseRouter.ridges().mapAll(this::wrap),
+            noiseRouter.ridges().mapAll(this::wrap)
+        };
+        DensityFunction[] cached = NativeClimateGrid.tryCreate(
+            functions,
+            noiseRouter,
+            this.firstNoiseX,
+            QuartPos.fromBlock(this.cellNoiseMinY * this.cellHeight),
+            this.firstNoiseZ,
+            this.noiseSizeXZ,
+            QuartPos.fromBlock(this.cellCountY * this.cellHeight),
+            this.noiseSizeXZ
+        );
+        if (cached != null) {
+            functions = cached;
+        }
+        return new Climate.Sampler(
+            functions[0],
+            functions[1],
+            functions[2],
+            functions[3],
+            functions[4],
+            functions[5],
             points
         );
     }
 
     protected @Nullable BlockState getInterpolatedState() {
-        return this.blockStateRule.calculate(this);
+        double density = this.finalDensity.compute(this);
+        if (this.activeBeardifier != null) {
+            density += this.activeBeardifier.compute(this);
+        }
+        BlockState blockState = this.aquifer.computeSubstance(this, density);
+        if (blockState != null) {
+            return blockState;
+        }
+        return this.oreVeinifier == null ? null : this.oreVeinifier.calculate(this);
     }
 
     @Override
@@ -212,7 +_,12 @@
     public int preliminarySurfaceLevel(int x, int z) {
         int blockPosCoord = QuartPos.toBlock(QuartPos.fromBlock(x));
         int blockPosCoord1 = QuartPos.toBlock(QuartPos.fromBlock(z));
-        return this.preliminarySurfaceLevelCache.computeIfAbsent(ColumnPos.asLong(blockPosCoord, blockPosCoord1), this::computePreliminarySurfaceLevel);
+        long key = ColumnPos.asLong(blockPosCoord, blockPosCoord1);
+        int cached = this.preliminarySurfaceLevelCache.get(key);
+        if (cached != Integer.MIN_VALUE) return cached;
+        int computed = this.computePreliminarySurfaceLevel(key);
+        this.preliminarySurfaceLevelCache.put(key, computed);
+        return computed;
     }
 
     private int computePreliminarySurfaceLevel(long packedChunkPos) {
@@ -312,16 +_,20 @@
     public void updateForY(int cellEndBlockY, double y) {
         this.inCellY = cellEndBlockY - this.cellStartBlockY;
 
-        for (NoiseChunk.NoiseInterpolator noiseInterpolator : this.interpolators) {
-            noiseInterpolator.updateForY(y);
+        for (NoiseChunk.NoiseInterpolator noiseInterpolator : this.interpolatorArray) {
+            noiseInterpolator.valueXZ00 = Mth.lerp(y, noiseInterpolator.noise000, noiseInterpolator.noise010);
+            noiseInterpolator.valueXZ10 = Mth.lerp(y, noiseInterpolator.noise100, noiseInterpolator.noise110);
+            noiseInterpolator.valueXZ01 = Mth.lerp(y, noiseInterpolator.noise001, noiseInterpolator.noise011);
+            noiseInterpolator.valueXZ11 = Mth.lerp(y, noiseInterpolator.noise101, noiseInterpolator.noise111);
         }
     }
 
     public void updateForX(int cellEndBlockX, double x) {
         this.inCellX = cellEndBlockX - this.cellStartBlockX;
 
-        for (NoiseChunk.NoiseInterpolator noiseInterpolator : this.interpolators) {
-            noiseInterpolator.updateForX(x);
+        for (NoiseChunk.NoiseInterpolator noiseInterpolator : this.interpolatorArray) {
+            noiseInterpolator.valueZ0 = Mth.lerp(x, noiseInterpolator.valueXZ00, noiseInterpolator.valueXZ10);
+            noiseInterpolator.valueZ1 = Mth.lerp(x, noiseInterpolator.valueXZ01, noiseInterpolator.valueXZ11);
         }
     }
 
@@ -329,8 +_,8 @@
         this.inCellZ = cellEndBlockZ - this.cellStartBlockZ;
         this.interpolationCounter++;
 
-        for (NoiseChunk.NoiseInterpolator noiseInterpolator : this.interpolators) {
-            noiseInterpolator.updateForZ(z);
+        for (NoiseChunk.NoiseInterpolator noiseInterpolator : this.interpolatorArray) {
+            noiseInterpolator.value = Mth.lerp(z, noiseInterpolator.valueZ0, noiseInterpolator.valueZ1);
         }
     }
 
@@ -371,7 +_,29 @@
     }
 
     protected DensityFunction wrap(DensityFunction densityFunction) {
-        return this.wrapped.computeIfAbsent(densityFunction, this::wrapNew);
+        if (densityFunction instanceof DensityFunctions.Marker) {
+            return this.wrapped.computeIfAbsent(densityFunction, this::wrapNew);
+        }
+
+        if (this.blender != Blender.empty()) {
+            if (densityFunction == DensityFunctions.BlendAlpha.INSTANCE) {
+                return this.blendAlpha;
+            }
+
+            if (densityFunction == DensityFunctions.BlendOffset.INSTANCE) {
+                return this.blendOffset;
+            }
+        }
+
+        if (densityFunction == DensityFunctions.BeardifierMarker.INSTANCE) {
+            return this.beardifier;
+        }
+
+        if (densityFunction instanceof DensityFunctions.HolderHolder holderHolder) {
+            return holderHolder.function().value();
+        }
+
+        return densityFunction;
     }
 
     private DensityFunction wrapNew(DensityFunction densityFunction) {
@@ -621,21 +_,48 @@
         private final DensityFunction noiseFiller;
         final double[] values;
         final int sizeXZ;
+        private boolean lastFallbackValid;
+        private int lastFallbackX;
+        private int lastFallbackY;
+        private int lastFallbackZ;
+        private double lastFallbackValue;
 
         FlatCache(final DensityFunction noiseFiller, final boolean computeValues) {
             this.noiseFiller = noiseFiller;
             this.sizeXZ = NoiseChunk.this.noiseSizeXZ + 1;
             this.values = new double[this.sizeXZ * this.sizeXZ];
             if (computeValues) {
-                for (int i = 0; i <= NoiseChunk.this.noiseSizeXZ; i++) {
-                    int i1 = NoiseChunk.this.firstNoiseX + i;
-                    int blockPosCoord = QuartPos.toBlock(i1);
-
-                    for (int i2 = 0; i2 <= NoiseChunk.this.noiseSizeXZ; i2++) {
-                        int i3 = NoiseChunk.this.firstNoiseZ + i2;
-                        int blockPosCoord1 = QuartPos.toBlock(i3);
-                        this.values[i + i2 * this.sizeXZ] = noiseFiller.compute(new DensityFunction.SinglePointContext(blockPosCoord, 0, blockPosCoord1));
+                int firstQuartX = NoiseChunk.this.firstNoiseX;
+                int firstQuartZ = NoiseChunk.this.firstNoiseZ;
+                boolean nativeFilled = NativeDensityFunction.tryFillGrid(
+                    this.values,
+                    noiseFiller,
+                    QuartPos.toBlock(firstQuartX),
+                    0.0,
+                    QuartPos.toBlock(firstQuartZ),
+                    4.0,
+                    1.0,
+                    4.0,
+                    firstQuartX,
+                    firstQuartZ,
+                    this.sizeXZ,
+                    1,
+                    this.sizeXZ
+                );
+                boolean checkParity = nativeFilled && NativeDensityFunction.shouldCheckParity();
+                double[] nativeValues = checkParity ? this.values.clone() : null;
+                if (!nativeFilled || checkParity) {
+                    for (int i = 0; i <= NoiseChunk.this.noiseSizeXZ; i++) {
+                        int blockPosCoord = QuartPos.toBlock(firstQuartX + i);
+                        for (int i2 = 0; i2 <= NoiseChunk.this.noiseSizeXZ; i2++) {
+                            int blockPosCoord1 = QuartPos.toBlock(firstQuartZ + i2);
+                            this.values[i + i2 * this.sizeXZ] = noiseFiller.compute(new DensityFunction.SinglePointContext(blockPosCoord, 0, blockPosCoord1));
+                        }
                     }
+                }
+                if (checkParity) {
+                    NativeDensityFunction.recordParity("flatGrid", noiseFiller, nativeValues, this.values);
+                    System.arraycopy(nativeValues, 0, this.values, 0, this.values.length);
                 }
             }
         }
@@ -646,7 +_,27 @@
             int quartPosZ = QuartPos.fromBlock(context.blockZ());
             int i = quartPosX - NoiseChunk.this.firstNoiseX;
             int i1 = quartPosZ - NoiseChunk.this.firstNoiseZ;
-            return i >= 0 && i1 >= 0 && i < this.sizeXZ && i1 < this.sizeXZ ? this.values[i + i1 * this.sizeXZ] : this.noiseFiller.compute(context);
+            if (i >= 0 && i1 >= 0 && i < this.sizeXZ && i1 < this.sizeXZ) {
+                return this.values[i + i1 * this.sizeXZ];
+            }
+
+            int blockX = context.blockX();
+            int blockY = context.blockY();
+            int blockZ = context.blockZ();
+            if (this.lastFallbackValid
+                && this.lastFallbackX == blockX
+                && this.lastFallbackY == blockY
+                && this.lastFallbackZ == blockZ) {
+                return this.lastFallbackValue;
+            }
+
+            double value = this.noiseFiller.compute(context);
+            this.lastFallbackValid = true;
+            this.lastFallbackX = blockX;
+            this.lastFallbackY = blockY;
+            this.lastFallbackZ = blockZ;
+            this.lastFallbackValue = value;
+            return value;
         }
 
         @Override
@@ -773,9 +_,61 @@
         @Override
         public void fillArray(double[] array, DensityFunction.ContextProvider contextProvider) {
             if (NoiseChunk.this.fillingCell) {
-                contextProvider.fillAllDirectly(array, this);
+                if (contextProvider == NoiseChunk.this) {
+                    lattice$fillCellArray(
+                        array,
+                        NoiseChunk.this.cellWidth,
+                        NoiseChunk.this.cellHeight,
+                        this.noise000,
+                        this.noise100,
+                        this.noise010,
+                        this.noise110,
+                        this.noise001,
+                        this.noise101,
+                        this.noise011,
+                        this.noise111
+                    );
+                    NoiseChunk.this.arrayIndex = NoiseChunk.this.cellWidth * NoiseChunk.this.cellWidth * NoiseChunk.this.cellHeight;
+                    NoiseChunk.this.inCellY = 0;
+                    NoiseChunk.this.inCellX = NoiseChunk.this.cellWidth - 1;
+                    NoiseChunk.this.inCellZ = NoiseChunk.this.cellWidth - 1;
+                } else {
+                    contextProvider.fillAllDirectly(array, this);
+                }
             } else {
                 this.wrapped().fillArray(array, contextProvider);
+            }
+        }
+
+        static void lattice$fillCellArray(
+            double[] array,
+            int cellWidth,
+            int cellHeight,
+            double noise000,
+            double noise100,
+            double noise010,
+            double noise110,
+            double noise001,
+            double noise101,
+            double noise011,
+            double noise111
+        ) {
+            int arrayIndex = 0;
+            for (int y = cellHeight - 1; y >= 0; y--) {
+                double yFraction = (double)y / cellHeight;
+                for (int x = 0; x < cellWidth; x++) {
+                    double xFraction = (double)x / cellWidth;
+                    double valueZ0 = Mth.lerp(
+                        yFraction, Mth.lerp(xFraction, noise000, noise100), Mth.lerp(xFraction, noise010, noise110)
+                    );
+                    double valueZ1 = Mth.lerp(
+                        yFraction, Mth.lerp(xFraction, noise001, noise101), Mth.lerp(xFraction, noise011, noise111)
+                    );
+
+                    for (int z = 0; z < cellWidth; z++) {
+                        array[arrayIndex++] = Mth.lerp((double)z / cellWidth, valueZ0, valueZ1);
+                    }
+                }
             }
         }
 
